# Cortex Visual Editor

Visually adjust spacing, radius, and gap in a running web app — then apply changes back to source code.

## How It Works

1. Run Cortex alongside your dev server
2. Click elements in the browser to select them
3. Pick design tokens (xs, sm, md, lg, xl) from the editing panel
4. See changes live, then click "Apply to Code" to update source files